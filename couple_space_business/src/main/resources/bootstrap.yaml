spring:
  cloud:
    nacos:
      config:
        server-addr:
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace: public
        file-extension: yaml
      discovery:
        enabled: true
        server-addr:
